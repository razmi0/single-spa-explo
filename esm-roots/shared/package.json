{
    "name": "@razmio/shared",
    "version": "1.0.0",
    "type": "module",
    "scripts": {
        "sync": "bun run sync.ts",
        "sync:watch": "bun run sync.ts --watch"
    },
    "devDependencies": {
        "@types/bun": "^1.1.0",
        "@types/node": "^20.0.0"
    }
}
