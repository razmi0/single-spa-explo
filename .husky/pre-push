npm run build
npm run benchmark -- --runs 3 --warmup 1

# Add benchmarks.json, commit it, then push will include this commit
echo "----"
echo "Adding benchmarks.json to commit"
git add bench/results/benchmarks.json
git commit -m "chore: updated benchmarks" || true
echo "Pushing to remote"
git push
