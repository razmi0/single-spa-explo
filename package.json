{
    "name": "mfe-bundler-exploration",
    "private": true,
    "scripts": {
        "build:shared": "cd esm-roots/shared && npm run build",
        "build:bench": "node bench/benchmark.mjs",
        "build": "npm run build:shared && npm run build:bench"
    }
}
